# PLANNING — 이 제품 자체의 기획 문서

이 파일은 "기획 하네스"입니다. 이 도구가 스스로 지원하는 PRD 템플릿(문제/근거/목표/하지 않을 것/해결 방향/리스크/출시 계획) 구조를 그대로 써서, 이 도구 자신의 기획을 기록합니다 — 새 기능을 넣기 전에 여기부터 갱신하세요. 세부 변경 이력(무엇을 왜 뺐는지/넓혔는지)은 `README.md`의 "기능을 버린다"/"범위를 넓힌다" 절에 남기고, 여기는 항상 "지금 시점의 최종 결론"만 유지합니다.

## 문제

PM이 새 문서(PRD, 회의록 등)를 쓸 때마다 템플릿을 처음부터 다시 세팅하거나, 예전에 쓴 문서를 뒤져서 복사해옵니다. 팀 내 다른 사람이 어떤 형식으로 문서를 쓰는지, 어떤 판단을 거쳐 결정했는지는 대개 그 사람 개인 문서함에만 남고 공유되지 않습니다.

## 근거

정량적 증거는 없음 — 가설 단계입니다. 실사용자 데이터가 아니라, 이 세션에서 사용자가 반복적으로 "문서 타입을 늘려달라", "여러 사람이 보는 커뮤니티/갤러리 형태로 만들어달라"고 요청한 것 자체를 방향성의 근거로 삼고 있습니다.

## 목표와 성공지표

정량 지표는 아직 없음 — 이 프로젝트는 실사용자가 없는 개인/강의 사례 프로젝트라 지표를 지어내지 않습니다. 정성적 목표만: "PM 한 명이 문서 작성부터 팀 공유까지 이 도구 하나로 끝낼 수 있는가."

## 하지 않을 것

- 실시간 공동 편집 (Google Docs 스타일) — 동시 편집 충돌 해결은 완전히 다른 아키텍처(CRDT/OT)가 필요해 범위가 너무 커짐
- 결제/유료 플랜 — 개인 도구 단계에서 불필요
- 다국어(i18n) — 사용자가 전부 한국어 사용자라는 전제
- 모바일 네이티브 앱 — 반응형 웹으로 충분 (에디터는 데스크톱 전용으로 이미 명시적으로 좁혀둠)
- AI의 실행형 동작(문서 밖 액션을 대신 실행) — AI는 항상 조회/제안형(초안 생성, 재작성, 지적)에 머무름. 이 경계는 처음부터 유지 중
- 커뮤니티 기능의 댓글/좋아요/팔로우 등 소셜 기능 — "공개해서 보여준다"까지만. 상호작용은 다음 이터레이션

## 해결 방향

세 단계로 확장했고, 각 단계는 이전 단계 위에 쌓이는 구조로 골랐습니다(동시에 넓히지 않음 — 한 번에 하나씩 검증):

1. **문서 타입 다양화** (완료) — PRD 하나만 지원하던 걸 여러 템플릿 등록/전환으로 넓힘. "PM을 위한 문서 도구"라는 포지셔닝의 최소 조건.
2. **공통 네비게이션 + 템플릿 갤러리** (완료) — 화면이 늘어나면서(설정/목록/작성/커뮤니티) 화면 간 이동 수단이 없던 문제가 실제로 드러남. 템플릿 선택도 `<select>` 하나로는 여러 문서 타입을 훑어보기 어려워져서 카드형 갤러리로 승격.
3. **공개 사례/커뮤니티 페이지** (완료, GitHub Pages 프리뷰 배포로 검증) — "팀 내 공유가 안 된다"는 원래 문제 정의를 직접 해결하는 단계. 검토했던 대안: (a) 팀 단위 비공개 공유 — 팀/조직 모델이 없는 지금 범위에는 과함, (b) Slack 연동 등 외부 공유 — 외부 서비스 의존이 생겨 범위가 커짐. 결국 "발행 여부만 토글하는 공개 읽기 전용 페이지"로 가장 좁게 골랐습니다.
4. **의사결정 지원 확장** (완료, GitHub Pages 프리뷰 배포로 검증) — 지금까지는 "문서를 쓰고 공유한다"까지였고, "무엇을 결정하는가"라는 강의 주제를 도구 기능으로 직접 지원하는 게 없었습니다. 다음을 추가합니다:
   - **템플릿 마켓플레이스 연결**: 커뮤니티에 발행된 스펙에서 "이 템플릿으로 새 스펙 만들기" — 이미 있는 템플릿 갤러리·커뮤니티 두 기능을 연결. 새 데이터 필드 불필요 (스펙에 이미 스냅샷된 `sections[].{key,title,hint}`를 그대로 템플릿으로 재사용).
   - **작성자 이름 표시** (미해결 질문 해소, 아래 참고): 익명 유지 대신 이름 노출로 결정.
   - **커뮤니티 검색/필터**: 문서 타입 기준 필터 + 제목/한줄정의 텍스트 검색. 클라이언트 사이드 필터로 충분 (별도 검색 인덱스 불필요할 규모).
   - **Firestore rules 자동화 테스트**: `VERIFICATION.md`가 지목한, 유일하게 이 하네스에서 바로 자동화 가능한 영역. `firebase emulators:exec` + `@firebase/rules-unit-testing`으로 공개/비공개 읽기·쓰기 회귀를 코드로 고정.
   - **새 기본 문서 타입 3종**: 기능 명세서 / 릴리즈 노트 / 실험 설계서(A-B 테스트 계획) — PRD·회의록 외에 PM-개발 협업에 실제로 필요한 문서. 기존 `DocTemplate` 구조 그대로 씀, 새 아키텍처 불필요.
   - **의사결정 로그** (신규 문서 타입): 결정 사항/배경/검토한 대안/선택 이유(트레이드오프)/되돌리기 조건 섹션. 이 강의 주제와 가장 직접 맞닿는 문서 타입.
   - **우선순위 스코어링(RICE/ICE)** (신규 문서 타입): 별도 계산 위젯을 만들지 않고, 기존 템플릿 구조로 hint에 "항목별 R·I·C·E 점수와 근거를 표로 적어보세요"처럼 안내만 하는 가이드형 문서로 구현 — 스프레드시트 기능을 텍스트 에디터 안에 새로 짓는 대신, "기능을 버린다" 원칙대로 범위를 텍스트 입력으로 좁힘.
   - **가정/리스크 트래커** (신규 데이터 구조): 유일하게 이번 확장에서 진짜 새 아키텍처가 필요한 항목 — 스펙마다 `{id, text, resolved}` 목록을 저장하고 체크로 해소 여부를 추적. 기존 "빈틈 지적" AI 기능이 그 자리에서 사라지던 결과물을 여기로 옮겨 담을 수 있게 연결.
   - **이해관계자 확인 체크리스트** (신규 데이터 구조, 리스크 트래커와 UI 패턴 공유): 스펙마다 `{id, name, confirmed}` 목록. 풀 코멘트/리뷰어 초대 시스템(이미 "기능을 버린다"에서 뺀 항목)은 여전히 안 만들고, "확인이 필요한 사람 목록과 확인 여부"만 가장 좁게 구현.

5. **개인 활동 분석** (완료) — "Amplitude 같은 걸 만들어줘"라는 요청에서 시작. 그대로 옮기면 여러 사용자의 서비스 전체를 보는 관리자 대시보드가 되는데, 이 도구는 각자 자기 uid로 로그인해 자기 데이터만 보는 구조(`firestore.rules`가 전부 본인 uid 기준)라 정면으로 충돌합니다. 그래서 **"내 활동 분석"** — 본인 이벤트만 본인이 보는 개인용 버전으로 좁혔습니다:
   - **이벤트 트래킹**: `events/{eventId}` 컬렉션에 `uid, name, properties, timestamp`. 스펙 생성/초안 생성/섹션 재작성/빈틈 지적/발행 토글/템플릿 가져오기 같은 기존 동작 지점에 `trackEvent()` 호출만 추가 — 새 백엔드 없이 기존 Firestore 직접 쓰기 패턴 그대로.
   - **대시보드**: DAU 대신 "내 일별 활동량" 추이, 이벤트 타입별 빈도.
   - **퍼널**: 전사 사용자 퍼널 대신 "내 문서가 초안 생성 → 발행까지 가는 전환율" (문서 타입별로 볼 수 있음).
   - **코호트/리텐션**: 여러 사용자 코호트 대신 "템플릿별로 스펙을 만든 뒤 나중에 다시 돌아와서 수정하는 비율" — 생성 주 기준으로 그룹핑.
   - **집계 방식**: Cloud Functions 트리거나 별도 집계 파이프라인을 새로 만들지 않고, 클라이언트가 자신의 이벤트를 읽어 브라우저에서 직접 집계합니다 — 개인 데이터 규모에서는 충분하고, "별도 백엔드 서버 없음" 원칙과 맞음.
   - **차트**: 새 차트 라이브러리를 추가하지 않고 인라인 SVG로 직접 그립니다 — 4개 화면 전부 "크기 비교"가 목적(식별이 목적인 다범주 비교가 아님)이라 기존 `primary` 파란 램프 하나만으로 충분하고, 방금 끝낸 번들 크기 최적화를 무의미하게 만들지 않기 위해서이기도 합니다.
   - **당시엔 하지 않았던 것**: 다른 사용자 데이터 열람(관리자 뷰) — 아래 6단계에서 다시 검토.

6. **관리자용 전체 사용자 분석** (진행 중) — 5단계를 배포한 뒤 "여러 사용자를 확인하는 것으로 넓혀줘"라는 요청이 왔습니다. 5단계에서 의도적으로 뺐던 항목을 다시 꺼내는 것이므로, 그때의 우려(프라이버시)를 정확히 어떻게 해소했는지 남깁니다:
   - **누가 볼 수 있는가**: 로그인한 아무나가 아니라 **특정 관리자 계정만** — 세 가지 선택지(관리자만/로그인한 누구나/집계값만 익명 공개) 중 가장 좁은 걸 골랐습니다. 프라이버시 리스크가 가장 작고, "관리자만 전체를 본다"는 실제 Amplitude의 권한 모델과도 가장 가깝습니다.
   - **관리자 목록 관리 방식**: `admins/{uid}` 컬렉션 — 문서가 존재하면 그 uid는 관리자. `firestore.rules`에서 이 컬렉션은 **쓰기를 완전히 막습니다**(관리자 본인도 못 씀) — 클라이언트 코드로 자기 자신을 관리자로 승격시키는 경로를 원천 차단하기 위함. 관리자 추가/제거는 Firebase 콘솔에서 문서를 직접 만들고 지우는 것으로만 가능 (README의 "사용자가 직접 해야 하는 것"에 추가).
   - **읽기 전용**: 관리자도 다른 사용자의 `events`/`specs`를 **읽기만** 가능하고 쓰기/수정/삭제는 여전히 본인 데이터에만 허용됩니다 — 관리자 계정이 뚫려도 피해가 "열람"으로 국한되도록 블라스트 반경을 최소화.
   - **UI**: 새 화면을 따로 만들지 않고, 기존 `/analytics`에 관리자에게만 보이는 "내 데이터 / 전체 사용자" 전환 토글을 추가 — 이미 만든 대시보드/퍼널/리텐션 3탭과 차트 컴포넌트를 그대로 재사용하고 데이터 소스만 "본인" → "전체"로 바뀝니다. 전체 사용자 보기에는 "사용자별 활동" 막대 차트를 하나 더 추가해 "누가 이 도구를 많이 쓰는지"를 보여줍니다(작성자 표시 이름 기준 — 커뮤니티 기능에서 이미 결정한 "이름은 노출, uid/이메일은 비노출" 원칙 재사용).

## 리스크와 미해결 질문

- **공개 범위 실수**: `spec.published`를 토글하는 순간 그 문서의 전체 내용(제품 설명 등 민감할 수 있는 내용 포함)이 비로그인 사용자에게도 보임. Firestore rules가 `published == true`일 때만 읽기를 허용하고, 쓰기는 여전히 작성자만 가능하도록 정확히 분리되어야 함 — 규칙 실수가 곧 데이터 노출 사고로 이어짐.
- **AI 프롬프트에 공개 여부가 스며들 위험**: `config.productContext`/`glossary`는 공개 스펙이라도 프롬프트 조립 시 그대로 들어가는데, 이게 공개 문서 자체에 노출되지는 않는지 (지금 구조상 productContext는 문서 내용에 직접 렌더링되지 않으므로 노출 안 됨 — 다만 다음에 "공개 문서에 제품 설명도 같이 보여주자"는 요청이 오면 이 경계를 다시 짚어야 함).
- **해소된 질문 → 작성자 이름 표시로 결정**: 커뮤니티 목록/상세에 작성자의 Google 표시 이름(`displayName`)을 노출하기로 결정. 스펙 생성 시점에 스냅샷으로 저장(스냅샷 원칙 재사용 — 나중에 사용자가 구글 계정 이름을 바꿔도 이미 발행된 문서엔 영향 없음). uid/이메일은 여전히 노출하지 않음 — 이름만. 구버전 스펙(필드 없음)은 "익명"으로 대체 표시.
- **새 리스크 (이번 확장으로 발생)**: 이름 노출은 uid보다 개인정보 범위가 넓음 — 실명을 쓰는 Google 계정이면 실명이 공개 문서에 그대로 남음. 지금 스코프에선 닉네임 설정 기능 없이 표시 이름을 그대로 씀 — 필요해지면 "닉네임 별도 설정" 기능으로 다음 이터레이션에 분리.
- **새 리스크**: 가정/리스크 트래커·이해관계자 체크리스트는 스펙 하위에 새 배열 필드를 추가함 — 기존 `updateSpec(id, data)`가 문서 전체 필드 단위 업데이트라 두 기능을 동시에 편집해도 Firestore merge 규칙상 서로 덮어쓰지 않는지 확인 필요 (배열 필드는 각각 독립적으로 `updateSpec(id, { risks })`/`updateSpec(id, { stakeholders })`처럼 분리 호출하면 안전).
- **새 리스크 (개인 활동 분석)**: `events`는 매 사용자 행동마다 문서가 쌓이는 append-only 로그라 `specs`/`config`보다 훨씬 빨리 커짐 — 개인 사용 규모에선 문제 없지만, `firestore.rules`에서 `update`/`delete`를 아예 막아 로그 위변조를 방지하고(생성만 허용), 읽기는 `request.auth.uid == resource.data.uid`로만 좁혀야 함(다른 사용자의 행동 패턴이 노출되면 그 자체로 프라이버시 사고).
- **새 리스크 (관리자 분석, 가장 중요)**: 관리자 판별 로직에 자기 자신을 관리자로 승격시키는 경로가 조금이라도 남으면 전체 사용자 데이터 노출로 직결됩니다. `admins/{uid}` 컬렉션의 쓰기 규칙을 `if false`로 완전히 막아(관리자 본인도 못 씀) 클라이언트 코드 경로로는 이 컬렉션에 절대 쓸 수 없게 했고, 오직 Firebase 콘솔에서 프로젝트 소유자가 직접 문서를 추가/삭제하는 것으로만 관리자 지정이 바뀝니다. `firestore.rules.test.js`에 "관리자 문서 쓰기는 관리자 본인 요청이어도 거부됨"을 명시적으로 테스트하세요 — 이 하나가 뚫리면 이번 확장 전체의 보안 전제가 무너집니다.

## 출시 계획

한 번에 전체 공개. 별도 단계적 롤아웃 없음 — 실사용자가 없는 개인 프로젝트라 카나리/점진적 공개가 의미 없음. 배포 경로는 README의 "사용자가 직접 해야 하는 것" 참고 (Firebase 프로젝트 생성 → `firebase deploy`). 되돌리는 방법: `firestore.rules`를 이전 버전(공개 읽기 규칙 제거)으로 되돌리고 재배포하면 커뮤니티 기능만 즉시 비활성화 가능.
