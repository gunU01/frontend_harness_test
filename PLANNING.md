# PLANNING — 이 제품 자체의 기획 문서

이 파일은 "기획 하네스"입니다. 이 도구가 스스로 지원하는 PRD 템플릿(문제/근거/목표/하지 않을 것/해결 방향/리스크/출시 계획) 구조를 그대로 써서, 이 도구 자신의 기획을 기록합니다 — 새 기능을 넣기 전에 여기부터 갱신하세요. 세부 변경 이력(무엇을 왜 뺐는지/넓혔는지)은 `README.md`의 "기능을 버린다"/"범위를 넓힌다" 절에 남기고, 여기는 항상 "지금 시점의 최종 결론"만 유지합니다.

## 문제

PM이 새 문서(PRD, 회의록 등)를 쓸 때마다 템플릿을 처음부터 다시 세팅하거나, 예전에 쓴 문서를 뒤져서 복사해옵니다. 팀 내 다른 사람이 어떤 형식으로 문서를 쓰는지, 어떤 판단을 거쳐 결정했는지는 대개 그 사람 개인 문서함에만 남고 공유되지 않습니다.

## 근거

정량적 증거는 없음 — 가설 단계입니다. 실사용자 데이터가 아니라, 이 세션에서 사용자가 반복적으로 "문서 타입을 늘려달라", "여러 사람이 보는 커뮤니티/갤러리 형태로 만들어달라"고 요청한 것 자체를 방향성의 근거로 삼고 있습니다.

## 목표와 성공지표

정량 지표는 아직 없음 — 이 프로젝트는 실사용자가 없는 개인/강의 사례 프로젝트라 지표를 지어내지 않습니다. 정성적 목표만: "PM 한 명이 문서 작성부터 팀 공유까지 이 도구 하나로 끝낼 수 있는가."

## 하지 않을 것

- 실시간 공동 편집 (Google Docs 스타일) — 동시 편집 충돌 해결은 완전히 다른 아키텍처(CRDT/OT)가 필요해 범위가 너무 커짐
- 결제/유료 플랜 — 개인 도구 단계에서 불필요
- 다국어(i18n) — 사용자가 전부 한국어 사용자라는 전제
- 모바일 네이티브 앱 — 반응형 웹으로 충분 (에디터는 데스크톱 전용으로 이미 명시적으로 좁혀둠)
- AI의 실행형 동작(문서 밖 액션을 대신 실행) — AI는 항상 조회/제안형(초안 생성, 재작성, 지적)에 머무름. 이 경계는 처음부터 유지 중
- 커뮤니티 기능의 댓글/좋아요/팔로우 등 소셜 기능 — "공개해서 보여준다"까지만. 상호작용은 다음 이터레이션

## 해결 방향

세 단계로 확장했고, 각 단계는 이전 단계 위에 쌓이는 구조로 골랐습니다(동시에 넓히지 않음 — 한 번에 하나씩 검증):

1. **문서 타입 다양화** (완료) — PRD 하나만 지원하던 걸 여러 템플릿 등록/전환으로 넓힘. "PM을 위한 문서 도구"라는 포지셔닝의 최소 조건.
2. **공통 네비게이션 + 템플릿 갤러리** (진행 중) — 화면이 늘어나면서(설정/목록/작성/커뮤니티) 화면 간 이동 수단이 없던 문제가 실제로 드러남. 템플릿 선택도 `<select>` 하나로는 여러 문서 타입을 훑어보기 어려워져서 카드형 갤러리로 승격.
3. **공개 사례/커뮤니티 페이지** (진행 중) — "팀 내 공유가 안 된다"는 원래 문제 정의를 직접 해결하는 단계. 검토했던 대안: (a) 팀 단위 비공개 공유 — 팀/조직 모델이 없는 지금 범위에는 과함, (b) Slack 연동 등 외부 공유 — 외부 서비스 의존이 생겨 범위가 커짐. 결국 "발행 여부만 토글하는 공개 읽기 전용 페이지"로 가장 좁게 골랐습니다.

## 리스크와 미해결 질문

- **공개 범위 실수**: `spec.published`를 토글하는 순간 그 문서의 전체 내용(제품 설명 등 민감할 수 있는 내용 포함)이 비로그인 사용자에게도 보임. Firestore rules가 `published == true`일 때만 읽기를 허용하고, 쓰기는 여전히 작성자만 가능하도록 정확히 분리되어야 함 — 규칙 실수가 곧 데이터 노출 사고로 이어짐.
- **AI 프롬프트에 공개 여부가 스며들 위험**: `config.productContext`/`glossary`는 공개 스펙이라도 프롬프트 조립 시 그대로 들어가는데, 이게 공개 문서 자체에 노출되지는 않는지 (지금 구조상 productContext는 문서 내용에 직접 렌더링되지 않으므로 노출 안 됨 — 다만 다음에 "공개 문서에 제품 설명도 같이 보여주자"는 요청이 오면 이 경계를 다시 짚어야 함).
- **미해결 질문**: 커뮤니티 목록에 작성자 이름을 보여줄지? (지금은 익명 — Firebase Auth 표시 이름을 노출하는 순간 개인정보 범위가 넓어짐. 아직 답을 정하지 않음, 다음 결정 사항.)

## 출시 계획

한 번에 전체 공개. 별도 단계적 롤아웃 없음 — 실사용자가 없는 개인 프로젝트라 카나리/점진적 공개가 의미 없음. 배포 경로는 README의 "사용자가 직접 해야 하는 것" 참고 (Firebase 프로젝트 생성 → `firebase deploy`). 되돌리는 방법: `firestore.rules`를 이전 버전(공개 읽기 규칙 제거)으로 되돌리고 재배포하면 커뮤니티 기능만 즉시 비활성화 가능.
