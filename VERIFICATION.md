# 자체 검증 시나리오

자동화 테스트 프레임워크(vitest/playwright)는 아직 없습니다 — 화면 대부분이 Firebase Auth/Firestore/Functions에 실시간으로 의존하고, 이 하네스 세션에는 실제 Firebase 프로젝트 자격 증명이 없어서 지금은 자동화보다 "무엇을 확인해야 하는가"를 정확히 정의해두는 쪽이 우선입니다. 아래는 `firebase deploy` 이후 사람이(또는 자격 증명이 있는 세션이) 직접 실행해서 체크하는 시나리오입니다. 자동화할 가치가 가장 큰 것부터 P0/P1/P2로 나눴습니다.

## 0. 정적 검증 (자동, 이미 하네스가 매 커밋마다 함)

- [ ] `npm run lint` — 통과
- [ ] `npx tsc --noEmit` — 통과 (PostToolUse 훅이 매 Edit/Write마다 이미 실행함)
- [ ] `npm run build` — 통과, `dist/` 생성 확인
- [ ] `node --check functions/index.js` — Functions 코드 문법 오류 없음

이 네 개는 코드만으로 확인 가능해서 이미 자동화되어 있습니다. 아래부터는 실제 로그인/DB가 필요합니다.

## P0 — 핵심 플로우 (이거 하나라도 깨지면 도구 자체가 무의미)

- [ ] **로그인**: `/signin`에서 "Google로 로그인" 클릭 → Google 계정 선택 → `/specs`로 리다이렉트됨
- [ ] **팝업 차단 폴백**: 팝업이 막힌 환경(모바일 브라우저 등)에서도 `signInWithRedirect`로 로그인이 끝까지 됨 (`SignIn.tsx`의 `auth/popup-blocked` 분기)
- [ ] **스펙 생성 → 초안 생성 → 저장**: `/specs`에서 문서 타입(PRD) 선택 → 제목·한줄정의 입력 → 만들기 → `/specs/:id`로 이동 → "초안 생성" 클릭 → 7개 섹션이 모두 채워짐 → 새로고침해도 내용이 남아있음 (Firestore에 실제로 저장됐는지가 핵심)
- [ ] **섹션 재작성**: 왼쪽에서 섹션 하나 선택 → "이 섹션 다시 쓰기" → 그 섹션만 바뀌고 다른 섹션은 그대로임
- [ ] **빈틈 지적**: "빈틈 지적" 클릭 → 질문 3개가 화면에 뜸 → 새로고침하면 사라짐 (로컬 상태일 뿐 저장 안 되는 게 의도된 동작)

## P1 — 이번에 넓힌 기능 (문서 타입 다양화)

- [ ] **문서 타입 추가**: `/settings`에서 "+ 새 문서 타입" → 이름 변경 → 섹션 추가 → 2초 후 "저장됨" 표시 → 새로고침해도 유지됨
- [ ] **문서 타입 삭제 방지**: 문서 타입이 1개만 남았을 때 삭제 버튼이 비활성화되어 있음 (`templates.length <= 1` 가드)
- [ ] **섹션 순서변경**: ↑/↓ 버튼으로 섹션 순서를 바꾸면 그 순서대로 저장됨
- [ ] **스펙 생성 시 타입 선택**: `/specs`에서 "새 스펙 만들기" → `/specs/new`로 이동 → 등록된 모든 문서 타입이 카드로 보이고, 카드를 클릭하면 제목/한줄정의 입력 단계로 넘어가며, 만들기를 누르면 선택한 타입의 섹션 구성대로 스펙이 만들어짐 (`NewSpec.tsx`)
- [ ] **다른 타입 선택**: `/specs/new`에서 타입 선택 후 "다른 타입 선택"을 누르면 라우트 이동 없이 타입 갤러리로 되돌아감
- [ ] **카드에 타입 라벨**: 목록의 각 카드에 문서 타입 이름(예: "회의록")이 상태뱃지와 별도로 표시됨
- [ ] **★ 스냅샷 격리 (가장 중요한 회귀 시나리오)**: PRD로 스펙 A를 하나 만든다 → `/settings`에서 PRD 템플릿의 섹션 hint나 제목을 바꾼다 → 스펙 A를 다시 열어본다 → **스펙 A의 섹션 제목/hint는 안 바뀌어 있어야 함** (바뀌어 있다면 `createSpec`이 스냅샷이 아니라 참조를 저장하는 회귀 버그)

## P1-b — 공통 헤더 (화면 간 이동)

- [ ] **네비게이션 이동**: 로그인 후 `/specs`, `/specs/new`, `/settings`로 헤더의 "스펙 목록"/"템플릿"/"설정" 링크를 눌러 각각 이동됨
- [ ] **활성 링크 강조**: 현재 페이지에 해당하는 헤더 링크가 `text-primary-600 font-medium`으로 강조되고 나머지는 `text-slate-600`임 (`useLocation` 기반)
- [ ] **로그아웃**: 헤더의 "로그아웃" 클릭 → `signOut` 호출 → `AuthContext`가 `user`를 `null`로 바꾸고 `PrivateRoute`가 `/signin`으로 리다이렉트함 (수동 navigate 없이 동작하는지 확인)
- [ ] **공개 페이지엔 헤더 없음**: `/`, `/signin`에서는 헤더가 보이지 않음 (`PrivateRoute` 바깥이라 자동으로 제외됨)
- [ ] **커뮤니티 링크**: 헤더의 "커뮤니티" 링크를 누르면 `/community`로 이동하고 목록이 보임 (`/community`, `/community/:id`는 `PrivateRoute` 바깥의 공개 라우트라 헤더 자체는 이 두 페이지에는 안 보임 — 헤더에서 다른 화면으로 이동할 때만 해당)

## P1-c — 커뮤니티 공개 (이번에 추가)

- [ ] **발행 토글**: `/specs/:id`에서 "커뮤니티에 공개하기" 체크 → 즉시(디바운스 없이) Firestore에 `published: true` 저장됨(새로고침해도 체크 유지) → 체크 옆에 "누구나 이 링크로 볼 수 있습니다"와 `/community/:id` 링크가 나타남
- [ ] **발행 취소**: 같은 체크박스를 해제하면 `published: false`로 저장되고, 로그아웃 상태에서 같은 `/community/:id` URL로 접근하면 더 이상 내용이 보이지 않고 "찾을 수 없거나 비공개 문서입니다"가 뜸
- [ ] **커뮤니티 목록**: 로그아웃 상태에서 `/community` 접속 → 로그인 화면으로 리다이렉트되지 않고(공개 라우트) 공개된 스펙만 카드로 보임, 비공개 스펙은 안 보임, 각 카드에 작성자 정보(이름/이메일/uid)가 전혀 없음
- [ ] **커뮤니티 상세**: `/community/:id`에서 로그인 없이 제목/한줄정의/문서타입/섹션별 내용이 읽기 전용으로 보이고 편집 컨트롤·AI 버튼이 전혀 없음
- [ ] **★ Firestore 규칙 회귀 (가장 중요)**: (Firebase 에뮬레이터 필요) 비로그인 요청으로 비공개 스펙(`published` 없음 또는 `false`)의 `specs/{id}`를 읽으면 거부됨. `published: true`인 문서는 비로그인으로도 읽히지만 그 문서에 `update`/`delete`를 비로그인 또는 타인 uid로 시도하면 거부됨. `config/{uid}`는 이번 변경으로 건드리지 않았으므로 여전히 본인 uid가 아니면 읽기/쓰기 전부 거부됨을 재확인. `firebase emulators:exec` + `@firebase/rules-unit-testing`으로 자동화하기 가장 좋은 후보 — 지금 세션엔 에뮬레이터/자격 증명이 없어 수동/별도 세션에서 확인 필요.
- [ ] **복합 색인**: `firebase deploy --only firestore:indexes` 이후 `/community` 목록이 에러 없이 로드됨 (배포 전에는 `where(published==true)+orderBy(updatedAt desc)` 조합이 콘솔에 색인 생성 링크 에러를 띄울 수 있음 — `firestore.indexes.json` 배포로 해결)

## P2 — 부가 동작

- [ ] **반응형 전환**: 브라우저 폭을 768px 아래로 줄이면 `/specs/:id`가 편집 UI 대신 "데스크톱에서 이어서 작성하세요" + 읽기 전용 뷰로 바뀜
- [ ] **구버전 config 마이그레이션**: (수동 재현이 어려우면 스킵 가능) Firestore 콘솔에서 어떤 유저의 `config/{uid}` 문서에서 `templates` 필드를 지워본다 → `/settings` 재접속 시 크래시 없이 기본 템플릿 2종으로 채워짐 (`getOrCreateConfig`의 마이그레이션 분기)
- [ ] **Firestore 보안 규칙**: (Firebase 에뮬레이터 필요) 로그인하지 않은 상태 또는 다른 uid로 `specs/{다른유저의specId}`를 읽기/쓰기 시도 → 거부됨. `firebase emulators:exec` + `@firebase/rules-unit-testing`으로 자동화할 수 있는 유일한 영역이라, 다음 이터레이션에서 실제 테스트 코드로 옮기기 가장 좋은 후보입니다.

## 앞으로 자동화한다면

Firebase 에뮬레이터(`firebase emulators:start`)를 하네스 SessionStart 훅에 추가하면, 위 P0/P1 시나리오 중 "로그인 팝업"을 뺀 나머지는 Playwright + 에뮬레이터로 실제 자동화가 가능합니다. Firestore rules 시나리오는 에뮬레이터만 있으면 UI 없이도 바로 테스트 코드로 옮길 수 있어서, 자동화 투자 대비 가장 이득이 큽니다.
